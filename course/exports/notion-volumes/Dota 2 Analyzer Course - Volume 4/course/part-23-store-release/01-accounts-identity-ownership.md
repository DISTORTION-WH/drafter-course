# Глава 1. Аккаунты, идентичность приложения и владение

## Результат главы

Вы создадите реестр всех учётных записей и идентификаторов так, чтобы проект не потерялся при смене компьютера, сотрудника или подрядчика.

## 1. Личное или организационное владение

До регистрации ответьте:

- кто юридически публикует приложение;
- кому принадлежит домен, код, cloud account и store listing;
- кто получает выплаты и отвечает за налоги;
- можно ли зарегистрировать организационный Apple/Google account в вашей юрисдикции;
- что произойдёт, если единственный владелец потеряет доступ.

Не регистрируйте коммерческий продукт на аккаунт случайного знакомого. Перенос приложения возможен не всегда без последствий и занимает время.

Для юридических, налоговых и санкционных ограничений получите консультацию специалиста своей страны. Курс не подменяет её.

## 2. Реестр аккаунтов

Создайте закрытый `operations/account-registry.md` без паролей:

| Система | Владелец | Recovery email | MFA | Billing owner | Резервный admin |
|---|---|---|---|---|---|
| домен/registrar | ... | ... | да | ... | ... |
| AWS Organization | ... | ... | да | ... | ... |
| GitHub organization | ... | ... | да | ... | ... |
| Expo/EAS | ... | ... | да | ... | ... |
| Google Play Console | ... | ... | да | ... | ... |
| Apple Developer/App Store Connect | ... | ... | да | ... | ... |
| support email | ... | ... | да | ... | ... |

Пароли и recovery codes храните в надёжном password manager/офлайн резерве, а не в Markdown.

## 3. Неизменяемые идентификаторы

Выберите один reverse-DNS namespace, которым владеете:

```text
Android applicationId: com.yourcompany.dotaanalyzer
iOS bundleIdentifier:  com.yourcompany.dotaanalyzer
Expo project slug:     dota-analyzer
URL scheme:            dotaanalyzer
```

`applicationId` и `bundleIdentifier` — техническая идентичность приложения в магазинах. После публикации их нельзя просто переименовать как название на экране: новый ID обычно означает новое приложение и потерю бесшовных обновлений.

Не используйте чужой домен или слово, создающее ложное впечатление официального продукта. Проведите trademark/name check.

## 4. Версии и build numbers

```text
marketing version: 1.0.0
Android versionCode: 1, 2, 3, ...
iOS buildNumber: 1, 2, 3, ...
```

`1.0.0` объясняет версию людям. `versionCode`/`buildNumber` отличают каждую загруженную сборку и должны увеличиваться. Один и тот же номер повторно магазин не примет.

Запишите правило:

```text
patch: исправление без изменения обещанного поведения
minor: совместимая новая возможность
major: существенная несовместимость/перезапуск продукта
build number: автоматически увеличивается для каждой store upload
```

## 5. Signing credentials

Подпись доказывает магазину, кто выпустил приложение. Потеря контроля над учётной записью или ключами может заблокировать обновления.

Android:

- включите Play App Signing;
- различайте upload key и app signing key;
- храните recovery information;
- не коммитьте keystore и пароли.

iOS:

- App Store Connect/Apple Developer roles выдавайте по минимуму;
- certificates и provisioning profiles можно управлять через EAS, но владелец должен понимать, где они находятся;
- не передавайте Account Holder role подрядчику.

## 6. Разделение окружений

Development app не должна случайно обращаться к production. Возможны отдельные IDs:

```text
com.yourcompany.dotaanalyzer.dev
com.yourcompany.dotaanalyzer.preview
com.yourcompany.dotaanalyzer
```

Если используете отдельные IDs, создайте отдельные OAuth redirect URIs, push credentials и deep links. Production bundle ID никогда не должен подписывать неизвестный CI.

## Практика

1. Зарегистрируйте домен и рабочий email под своим контролем.
2. Заполните account registry без секретов.
3. Выберите IDs и проверьте их отсутствие в существующих store apps.
4. Настройте MFA и резервного владельца там, где доступно.
5. Создайте `docs/release/versioning.md`.
6. Выполните восстановление доступа в тестовом/безопасном сценарии.

## Готово, если

- юридический издатель и billing owner определены;
- IDs зафиксированы до первой загрузки;
- MFA включена;
- ключи не лежат в Git;
- существует recovery plan;
- development/production невозможно перепутать молча.

